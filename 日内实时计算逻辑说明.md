# 日内实时计算逻辑说明

## 核心原则
**以今日零点快照为基础进行计算**：如果是 16 日，则从 16 日 00:00 的快照数据作为初始持仓，然后加上 16 日 00:00 到当前时间的成交记录来计算实时指标。

## 执行频率
每 5 分钟执行一次 `_calculate_and_store_metrics`

## 计算流程

### 1. 初始化阶段（每次运行都执行）
- **重建所有零点快照**：调用 `_rebuild_midnight_snapshots`，从成交表重算所有历史 00:00 快照并覆盖写入 `position_metrics` 表
- **获取昨日收盘持仓**：从 `position_metrics` 表读取昨日 00:00 快照的 `left_*` 字段作为今日初始持仓

### 2. 检查今天零点快照
- 如果今天 00:00 快照不存在，再次调用 `_rebuild_midnight_snapshots` 重建
- **从今日零点快照读取初始持仓**：读取今日 00:00 快照的 `left_*` 字段作为今日计算的起点
  - `initial_long_qty = 今日零点快照的 left_long_qty`（即昨日收盘持仓）
  - `initial_short_qty = 今日零点快照的 left_short_qty`
  - `initial_long_value = 今日零点快照的 left_long_value`
  - `initial_short_value = 今日零点快照的 left_short_value`

### 3. 计算今日实时数据
- **初始持仓**：使用从今日零点快照读取的持仓数据
- **今日成交**：查询 `xt_trade_update` 表，统计 `00:00 -> 当前时间` 的成交记录
- **调用方法**：`calc.calculate_positions_by_symbol(start_time=今日00:00, end_time=当前时间, initial_positions_dict=从今日零点快照读取的持仓)`

### 4. 计算昨日数据（用于显示）
- 调用 `calculate_yesterday_end_left_qty_value`，计算昨日收盘时的持仓数据（仅用于日志显示）

### 5. 计算今日指标（基于 `calculate_positions_by_symbol` 的结果）

#### 5.1 总持仓计算
```
long_qty = initial_long_qty + buy_volume          # 初始多头持仓 + 今日买入量
short_qty = initial_short_qty + sell_volume      # 初始空头持仓 + 今日卖出量
long_value = initial_long_value + buy_trade_value # 初始多头市值 + 今日买入市值
short_value = initial_short_value + sell_trade_value # 初始空头市值 + 今日卖出市值
```

#### 5.2 平均价格计算
```
avg_buy_prz = long_value / long_qty              # 基于总持仓（初始+今日）的平均买价
avg_sell_prz = short_value / short_qty           # 基于总持仓（初始+今日）的平均卖价
```

#### 5.3 轧差和已实现盈亏
```
matched_qty = min(long_qty, short_qty)            # 总轧差数量（包含初始持仓+今日成交）
midnight_matched_qty = 从今日零点快照读取 matched_qty  # 昨日收盘时的轧差
daily_new_matched_qty = matched_qty - midnight_matched_qty  # 今日新增轧差
today_realized_pnl = daily_new_matched_qty * (avg_sell_prz - avg_buy_prz)  # 今日新增已实现盈亏
```

#### 5.4 剩余持仓和未实现盈亏
```
left_long_qty = long_qty - matched_qty
left_short_qty = short_qty - matched_qty
left_long_value = left_long_qty * avg_buy_prz
left_short_value = left_short_qty * avg_sell_prz
unrealized_pnl = left_long_qty * (close_prz - avg_buy_prz) + left_short_qty * (avg_sell_prz - close_prz)
```

#### 5.5 累计盈亏
```
cumulative_realized_pnl_at_midnight = 从今日零点快照读取 cumulative_realized_pnl  # 昨日收盘时的累计已实现盈亏
cumulative_realized_pnl_now = cumulative_realized_pnl_at_midnight + today_realized_pnl  # 当前累计已实现盈亏
cumulative_pnl = cumulative_realized_pnl_now + today_unrealized_pnl  # 累计总盈亏
```

### 6. 存储到数据库
- 将计算结果写入 `position_metrics` 表（`timestamp = 当前时间`）
- 更新 Prometheus metrics

## ✅ 已修复的问题

### 修复 1：统一数据源
- **之前**：从昨日计算结果（`calculate_yesterday_end_left_qty_value`）获取初始持仓
- **现在**：直接从今日零点快照读取初始持仓、`matched_qty` 和 `cumulative_realized_pnl`
- **好处**：确保数据源一致，避免计算误差累积

### 修复 2：统一基准值
- **之前**：使用昨日计算结果的 `matched_qty` 作为基准
- **现在**：使用今日零点快照的 `matched_qty` 作为基准（即昨日收盘时的轧差）
- **好处**：确保今日新增轧差的计算准确

## ⚠️ 仍需注意的问题

### `avg_sell_prz` 计算方式差异

**实时计算中：**
```
avg_sell_prz = (initial_short_value + sell_trade_value) / (initial_short_qty + sell_volume)
```
这是基于**总持仓**（初始+今日）的平均卖价。

**零点快照中：**
```
avg_sell_prz = daily_sell_value / daily_sell_volume
```
这是基于**当日卖出**的平均卖价（不包括初始持仓）。

**当前状态：**
- 虽然数据源已统一（都从今日零点快照读取初始持仓），但 `avg_sell_prz` 的计算方式仍然不同
- 如果初始持仓中有空头持仓，实时计算的 `avg_sell_prz` 会与零点快照不一致
- 这可能导致 `today_realized_pnl` 的计算结果与零点快照的 `daily_realized_pnl` 存在差异

**建议（可选）：**
- 如果需要完全一致，可以考虑在实时计算中也使用 `avg_sell_prz = sell_trade_value / sell_volume`（只基于今日卖出）
- 但这需要修改 `calculate_positions_by_symbol` 的逻辑
